#include "library/Param.h"
#include <boost/test/unit_test.hpp>
#include <iostream>

// Ditch this

BOOST_AUTO_TEST_CASE(paramTest)
{
    BOOST_CHECK_EQUAL(1, 1);
}

// Should have a test to make sure that the parameters are read in correctly
// And checked correctly

// If the parameter file name is invalid, doMain should crash
// If the parameter file name is valid, check that the parameters are read-in
// succesfully
// If the parameter names are not correct, should catch that
// If the parameter values are invalid, should catch that


